# THIS FILE IS AUTOGENERATED BY SYNCER DO NOT EDIT
name: Test for sync changes

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  check:
    name: Check for changes
    runs-on: ubuntu-latest
    steps:
      - name: Check out code (without token)
        uses: actions/checkout@v4
      - name: Check for changes
        if: steps.get_workflow_token.outputs.token == ''
        uses: getsyncer/action-syncer/checkpr@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
